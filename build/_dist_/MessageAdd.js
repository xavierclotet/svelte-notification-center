/* src/MessageAdd.svelte generated by Svelte v3.23.2 */
import {
	SvelteComponent,
	append,
	attr,
	detach,
	element,
	init,
	insert,
	listen,
	noop,
	prevent_default,
	run_all,
	safe_not_equal,
	set_input_value,
	space
} from "/web_modules/svelte/internal.js";

import { center } from "./notifications.js";

function create_fragment(ctx) {
	let h2;
	let t1;
	let form;
	let input;
	let t2;
	let button;
	let mounted;
	let dispose;

	return {
		c() {
			h2 = element("h2");
			h2.textContent = "Add new message";
			t1 = space();
			form = element("form");
			input = element("input");
			t2 = space();
			button = element("button");
			button.textContent = "new message";
			attr(h2, "class", "text-2xl font-medium");
			attr(input, "type", "text");
			attr(input, "class", "w-1/2 p-2");
			attr(input, "placeholder", "Enter your message");
			attr(button, "class", "px-5 py-2 text-gray-200 bg-gray-700");
			attr(button, "type", "submit");
			attr(form, "class", "mt-3");
		},
		m(target, anchor) {
			insert(target, h2, anchor);
			insert(target, t1, anchor);
			insert(target, form, anchor);
			append(form, input);
			set_input_value(input, /*value*/ ctx[0]);
			append(form, t2);
			append(form, button);

			if (!mounted) {
				dispose = [
					listen(input, "input", /*input_input_handler*/ ctx[2]),
					listen(form, "submit", prevent_default(/*addMessage*/ ctx[1]))
				];

				mounted = true;
			}
		},
		p(ctx, [dirty]) {
			if (dirty & /*value*/ 1 && input.value !== /*value*/ ctx[0]) {
				set_input_value(input, /*value*/ ctx[0]);
			}
		},
		i: noop,
		o: noop,
		d(detaching) {
			if (detaching) detach(h2);
			if (detaching) detach(t1);
			if (detaching) detach(form);
			mounted = false;
			run_all(dispose);
		}
	};
}

function instance($$self, $$props, $$invalidate) {
	let value = "";

	const addMessage = () => {
		if (!value) return;
		center.add(value);
		$$invalidate(0, value = "");
	};

	function input_input_handler() {
		value = this.value;
		$$invalidate(0, value);
	}

	return [value, addMessage, input_input_handler];
}

class MessageAdd extends SvelteComponent {
	constructor(options) {
		super();
		init(this, options, instance, create_fragment, safe_not_equal, {});
	}
}

export default MessageAdd;